using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class IsOnFocus : MonoBehaviour {//Script à mettre sur l'object initial du modele.

    public bool isOnFocus;
	// Use this for initialization
	void Start () {
        isOnFocus = false;
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
